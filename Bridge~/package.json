{
  "name": "jtlsdk-bridge",
  "version": "0.1.0",
  "private": true,
  "description": "JavaScript bridge of JTL SDK, built into Unity .jspre files.",
  "scripts": {
    "check": "tsc --noEmit",
    "build": "npm run check && node build.mjs",
    "build:yandex": "node build.mjs yandex",
    "build:youtube": "node build.mjs youtube"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "typescript": "^5.6.0"
  }
}
